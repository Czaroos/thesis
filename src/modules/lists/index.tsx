import React from "react";

import { Layout } from "@components";

import "./style.scss";

const Lists = () => {
  return <Layout sidebarChildren={<div />} contentChildren={<div />} />;
};

export default Lists;
